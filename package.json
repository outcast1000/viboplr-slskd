{
  "private": true,
  "name": "viboplr-slskd-tests",
  "description": "Test harness for the viboplr-slskd plugin (not published; the plugin itself is just index.js + manifest.json).",
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js",
    "package": "scripts/package.sh"
  }
}
